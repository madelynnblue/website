<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" href="/public/favicon.ico" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{{ title }}</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=IBM+Plex+Mono:wght@400;500;600&family=Crimson+Pro:wght@400;600&display=swap"
      rel="stylesheet"
    />
    <style>
      :root {
        --bg: #fafaf8;
        --text: #1a1a1a;
        --link: #0000ee;
        --link-visited: #551a8b;
        --border: #d4d4d4;
        --accent: #ff6b35;
        --code-bg: #f5f5f5;
        --mono: "IBM Plex Mono", "Courier New", monospace;
        --serif: "Crimson Pro", "Georgia", serif;
      }

      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      body {
        font-family: var(--serif);
        font-size: 18px;
        line-height: 1.7;
        color: var(--text);
        background: var(--bg);
        padding: 20px;
        max-width: 680px;
        margin: 0 auto;
      }

      header {
        border-bottom: 2px solid var(--border);
        padding-bottom: 20px;
        margin-bottom: 40px;
      }

      .post-header {
        margin-bottom: 20px;
      }

      h1 {
        font-family: var(--mono);
        font-size: 24px;
        font-weight: 600;
        margin-bottom: 8px;
        letter-spacing: -0.5px;
      }

      h2 {
        font-family: var(--mono);
        font-size: 18px;
        font-weight: 600;
        margin-bottom: 20px;
        text-transform: lowercase;
        border-left: 3px solid var(--accent);
        padding-left: 12px;
      }

      .post-meta {
        font-family: var(--mono);
        font-size: 14px;
        color: #888;
        display: flex;
        gap: 16px;
        flex-wrap: wrap;
      }

      .post-meta span {
        display: inline-block;
      }

      .post-content {
        margin-bottom: 60px;
      }

      .post-content h2 {
        font-size: 24px;
        font-weight: 600;
        margin-top: 48px;
        margin-bottom: 20px;
        line-height: 1.3;
      }

      .post-content h3 {
        font-size: 20px;
        font-weight: 600;
        margin-top: 36px;
        margin-bottom: 16px;
        line-height: 1.4;
      }

      .post-content p {
        margin-bottom: 20px;
      }

      .post-content a {
        color: var(--link);
        text-decoration: none;
        border-bottom: 1px solid var(--link);
        transition: all 0.2s;
      }

      .post-content a:visited {
        color: var(--link-visited);
        border-bottom-color: var(--link-visited);
      }

      .post-content a:hover {
        background: #ffe8e0;
        border-bottom-color: var(--accent);
        color: var(--accent);
      }

      .post-content ul,
      .post-content ol {
        margin-left: 24px;
        margin-bottom: 20px;
      }

      .post-content li {
        margin-bottom: 10px;
      }

      .post-content code {
        font-family: var(--mono);
        font-size: 15px;
        background: var(--code-bg);
        padding: 2px 6px;
        border-radius: 3px;
      }

      .post-content pre {
        background: var(--code-bg);
        border: 1px solid var(--border);
        border-left: 3px solid var(--accent);
        padding: 16px;
        overflow-x: auto;
        margin-bottom: 20px;
        border-radius: 3px;
      }

      .post-content pre code {
        background: none;
        padding: 0;
        font-size: 14px;
        line-height: 1.5;
      }

      .post-content blockquote {
        border-left: 3px solid var(--border);
        padding-left: 20px;
        margin: 24px 0;
        color: #666;
        font-style: italic;
      }

      .post-content hr {
        border: none;
        border-top: 1px solid var(--border);
        margin: 40px 0;
      }

      .post-content img {
        max-width: 100%;
        height: auto;
        margin: 30px 0;
        border: 1px solid var(--border);
      }

      p {
        margin-bottom: 16px;
      }

      a {
        color: var(--link);
        text-decoration: underline;
        transition: color 0.2s;
      }

      a:visited {
        color: var(--link-visited);
      }

      a:hover {
        color: var(--accent);
      }

      footer {
        margin-top: 60px;
        padding-top: 20px;
        border-top: 2px solid var(--border);
        font-family: var(--mono);
        font-size: 13px;
        color: #888;
        text-align: center;
      }

      footer a {
        color: #888;
        margin: 0 8px;
      }

      hr {
        border: none;
        border-top: 1px solid var(--border);
        margin: 40px 0;
      }

      code {
        font-family: var(--mono);
        font-size: 15px;
        background: var(--code-bg);
        padding: 2px 6px;
        border-radius: 3px;
      }

      blockquote {
        border-left: 3px solid var(--border);
        padding-left: 20px;
        margin: 20px 0;
        color: #666;
        font-style: italic;
      }

      ul,
      ol {
        margin-left: 24px;
        margin-bottom: 16px;
      }

      li {
        margin-bottom: 8px;
      }

      @media (max-width: 600px) {
        body {
          font-size: 16px;
          padding: 15px;
        }

        h1 {
          font-size: 20px;
        }

        h2 {
          font-size: 16px;
        }

        .post-content h2 {
          font-size: 20px;
        }

        .post-content h3 {
          font-size: 18px;
        }

        nav a {
          display: inline-block;
          margin-bottom: 8px;
        }
      }

      /* Link underline on hover only for body links */
      main a {
        text-decoration: none;
        border-bottom: 1px solid var(--link);
      }

      main a:visited {
        border-bottom-color: var(--link-visited);
      }

      main a:hover {
        background: #ffe8e0;
        border-bottom-color: var(--accent);
      }
    </style>
  </head>
  <body>
    <header>
      <h1>{{ title }}</h1>
    </header>
    <main>{{ content | safe }}</main>
    <footer>
      <p>
        <a href="/">home</a> · <a href="/me">me</a> ·
        <a href="/now">now</a>
      </p>
    </footer>
  </body>
</html>
